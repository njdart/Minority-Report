# The flag below indicates whether the Kinect's messages (about board obstruction) actually
# causes a canvas to be generated or not. (For debug purposes.)
#
# The flag can be toggled by emitting toggle_kinect_enable.
kinectEnable = True